name: GH Event Debugging

on:
  push:
    paths-ignore:
      - ".github/workflows/**"

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:

      - name: Run a one-line script
        run: |
          echo "${{ github.event }}"

